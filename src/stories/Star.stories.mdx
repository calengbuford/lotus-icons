import Star from '../components/Star'
import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs'

<Meta
  title="SVGs/Star"
  component={Star}
  parameters={{
    backgrounds: {
      default: 'lightGray',
      values: [{ name: 'lightGray', value: '#eee' }],
    },
  }}
  argTypes={{
    children: {
      control: false,
    },
    component: {
      control: 'select',
      options: ['div', 'section', 'aside'],
    },
  }}
/>

export const Template = (args) => <Star {...args} />

# Star

<Description of={Star} />

### Import

```ts
import { Star } from '@neiybor/lotus-icon-library'
```

### Basic

<Canvas>
  <Story name="Simple" args={{ style: { width: 200, height: 200 } }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Simple" />

## Demos

### Variants

<Canvas>
  <Story name="Variants">
    <div style={{ display: 'flex', gap: 20 }}>
      {/* <Paper style={{ width: 200, height: 200 }} /> */}
      {/* <Paper style={{ width: 200, height: 200 }} variant="outlined" /> */}
      {/* <Paper style={{ width: 200, height: 200 }} variant="outlined" square /> */}
    </div>
  </Story>
</Canvas>